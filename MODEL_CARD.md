# Model Card

> 이 GitHub 저장소는 코드와 문서만 공개합니다. 아래에서 설명하는 학습 가중치, 검색 인덱스, corpus 및 샘플별 평가 데이터는 포함되어 있지 않습니다.

## 개요

이 저장소는 하나의 단일 모델이 아니라 검색 모델과 세 가지 LoRA 어댑터를 연결한 한국어 특허 심사 보조 파이프라인입니다.

최종 판정 규칙은 다음과 같습니다.

```text
등록 = 신규성 만족 AND 진보성 만족
거절 = 그 외 모든 경우
```

## 구성요소

| 구성요소 | 기반 모델 | 산출물 | 역할 |
|---|---|---|---|
| 선행기술 검색 | `jhgan/ko-sroberta-multitask` | 전체 SBERT 모델 | 약 29,794건 corpus에서 top-2 검색 |
| 신규성 판단 | `unsloth/Qwen2.5-32B-Instruct` | LoRA r=32 | 신규성 만족/부족 |
| 진보성 판단 | `unsloth/Qwen2.5-32B-Instruct` | LoRA r=32 | 진보성 만족/부족 |
| 거절 세부 유형 | `Qwen/Qwen2.5-7B-Instruct` | 멀티라벨 sequence-classification LoRA r=16 | 결합류, 차이류, 설계변경, 공지기술부가 |
| 결과 설명 | `unsloth/Qwen2.5-32B-Instruct` | 기반 모델 | 판정과 검색 문헌을 바탕으로 한국어 설명 |

## 입력과 출력

권장 입력은 발명의 명칭, 초록, 청구항 1입니다. 파이프라인은 검색된 선행문헌 2건을 결합해 신규성과 진보성을 판단합니다.

대표 출력:

- 신규성: 만족 또는 부족
- 진보성: 만족 또는 부족
- 최종 판정: 등록 또는 거절
- 선택적 거절 세부 유형
- 검색된 선행기술과 유사도
- 사용자용 설명

## 보고된 성능

아래 수치는 저장소의 `END/FINAL_PATENT_PIPELINE_REPORT.md`에 기록된 결과입니다. 샘플별 평가 원문과 상세 JSON 결과는 공개 저장소에서 제외했습니다.

| 평가 항목 | 보고된 값 |
|---|---:|
| 최종 등록/거절 정확도 | 87.5% |
| 타이트 정확도 | 86.3% |
| 신규성 어댑터 정확도 | 84.0% |
| 진보성 어댑터 정확도 | 91.0% |
| SBERT soft Hit@2, random p99.5 기준 | 85.1% |
| 서브라벨 Hit@2 | 92.7% |

최종 정확도는 등록 500건과 거절 500건으로 구성된 1,000건 평가셋에서 측정되었다고 보고되어 있습니다. 최종 평가는 정답 선행문헌을 직접 주는 조건이 아니라 SBERT 검색 결과를 다음 단계에 전달하는 end-to-end 조건입니다.

SBERT의 정확한 문헌 ID 기준 strict recall@2는 40.67%입니다. 85.1%는 검색 문헌과 정답 선행문헌의 임베딩 유사도를 사용한 soft 기준이므로 strict recall과 동일하게 해석하면 안 됩니다.

## 학습 정보

- 신규성 학습: 신규성 부족 10,000건, 신규성 만족 10,000건
- 진보성 학습: 진보성 부족 10,000건, 진보성 만족 10,000건
- SBERT Stage 1: positive pair 기반 MNRL
- SBERT Stage 2: hard negative를 포함한 Cached MNRL
- 32B LoRA: BF16 일반 LoRA, response-only SFT로 보고됨
- 7B LoRA: 4개 라벨의 멀티라벨 sequence classification

원본 학습 데이터 전체는 이 저장소에 포함되어 있지 않습니다. `END/reconstructed_training`의 코드는 남아 있는 설정과 보고서를 바탕으로 재구성되었으며 원본 학습 코드를 완전히 복원한 것이 아닙니다. 별도로 보관되어 있던 일부 학습 스크립트는 `lora_sbert_training_code_bundle`에 포함되어 있습니다.

## 의도한 사용

- 한국어 특허 문헌의 연구용 선행기술 후보 검색
- 신규성·진보성 분류 실험
- 특허 분석 워크플로의 보조 신호
- 모델 및 검색 파이프라인 재현 연구

## 의도하지 않은 사용

- 변리사 또는 변호사의 검토를 대체하는 자동 법률 판단
- 특허 등록 가능성 보장
- 검색 결과에 포함되지 않은 사실을 근거로 한 판단
- 사람의 확인 없이 권리 취득·거절 대응 등 중요한 의사결정에 직접 사용

## 한계와 위험

- 평가셋이 균형 구성되어 실제 특허 출원 분포와 다를 수 있습니다.
- 검색 오류가 신규성·진보성 판단에 연쇄적으로 영향을 줍니다.
- 유사한 문헌을 찾는 것과 법적으로 적절한 선행기술을 확정하는 것은 다릅니다.
- 생성 설명은 잘못되거나 과도하게 확신하는 표현을 만들 수 있습니다.
- 주 입력이 청구항 1 중심이므로 종속항 전체를 충분히 반영하지 못할 수 있습니다.
- 학습 데이터 원문과 전체 데이터 생성 절차가 포함되어 있지 않아 완전한 재학습 재현에는 한계가 있습니다.
- BF16 32B 모델과 7B 분류기를 사용하므로 높은 GPU 메모리와 디스크 용량이 필요합니다.

## 라이선스

프로젝트 라이선스는 아직 지정되지 않았습니다. 기반 모델, 학습 산출물, 검색 corpus의 재배포 및 사용 조건을 각각 확인해야 합니다.

- `unsloth/Qwen2.5-32B-Instruct`: 모델 페이지 표시 기준 Apache-2.0
- `Qwen/Qwen2.5-7B-Instruct`: 모델 페이지 표시 기준 Apache-2.0
- `jhgan/ko-sroberta-multitask`: 모델 페이지에서 라이선스 표기를 확인하지 못함

기반 모델의 라이선스와 이 프로젝트의 코드·데이터·학습 산출물 라이선스는 별개의 문제입니다.
